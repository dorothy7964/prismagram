type Mutation {
    createAccount(
        userName: String!
        email: String!
        firstName: String
        lastName: String 
        bio: String
        avatar: String
    ): User!
}

type Query {
    something: String!
}